export class Unit {
  constructor (id, type, hp, maxHp, row, atk, def, status, team) {
    this.id = id
    this.type = type
    this.hp = hp
    this.maxHp = maxHp
    this.row = row
    this.atk = atk
    this.def = def
    this.status = status
    this.team = team
  }
}
